---
description:
globs:
alwaysApply: false
---
# RivenEmacs 开发规范

## 代码风格

### Emacs Lisp 编码规范

1. **文件头**: 每个配置文件应包含标准的文件头
   ```elisp
   ;;; <文件名>.el --- <描述> -*- lexical-binding: t; -*-
   ```

2. **词法绑定**: 始终使用 `lexical-binding: t`

3. **模块提供**: 每个模块文件末尾应包含 `(provide 'module-name)`

4. **文档字符串**: 为所有自定义函数和变量提供文档字符串

5. **命名约定**: 
   - 使用前缀避免命名冲突
   - 私有函数使用 `--` 前缀
   - 配置相关函数使用 `config:` 前缀

### use-package 使用规范

1. **基本结构**:
   ```elisp
   (use-package package-name
     :ensure t
     :defer t
     :init
     ;; 初始化代码
     :config
     ;; 配置代码
     :bind
     ;; 按键绑定
     :hook
     ;; 钩子设置
     )
   ```

2. **延迟加载**: 默认使用 `:defer t`，必要时使用 `:demand t`

3. **确保安装**: 始终使用 `:ensure t`

## 性能优化

### 启动优化

1. **垃圾回收**: 在 [early-init.el](mdc:early-init.el) 中配置 GC 优化
2. **延迟加载**: 最大化使用 `use-package` 的延迟加载特性
3. **文件处理**: 启动时暂时禁用 `file-name-handler-alist`

### 运行时优化

1. **读取缓冲区**: 设置 `read-process-output-max`
2. **字体缓存**: 设置 `inhibit-compacting-font-caches`
3. **原生编译**: 配置 `native-comp` 相关选项

## 模块化原则

1. **单一职责**: 每个模块文件只负责一个功能域
2. **依赖隔离**: 避免模块间的强耦合
3. **配置分离**: 基础配置和特定功能配置分离
4. **按需加载**: 使用适当的加载触发器

## 按键绑定管理

1. **使用 General**: 在 `init-general.el` 中配置按键绑定框架
2. **统一管理**: 主要按键绑定在 `init-keybindings.el` 中统一管理
3. **模块绑定**: 模块特定的按键绑定在对应模块文件中配置

## 错误处理

1. **警告抑制**: 在 [early-init.el](mdc:early-init.el) 中配置警告级别
2. **错误恢复**: 使用 `ignore-errors` 和 `condition-case` 处理潜在错误
3. **调试信息**: 必要时启用详细的调试信息

## 版本兼容性

1. **Emacs 版本**: 主要支持 Emacs 27+
2. **向后兼容**: 对于低版本 Emacs 的特殊处理
3. **包版本**: 在必要时指定包的版本要求

## 提交规范

1. **提交信息**: 自动生成的提交信息应包含 "[AI]" 前缀
2. **变更说明**: 清晰描述配置变更的目的和影响
3. **测试**: 确保配置变更不会破坏现有功能
