---
description:
globs:
alwaysApply: false
---
# RivenEmacs 项目结构指南

RivenEmacs 是一个模块化的 Emacs 配置项目，具有清晰的目录结构和配置管理。

## 核心配置文件

- **[early-init.el](mdc:early-init.el)** - Emacs 早期初始化配置，包含性能优化、包管理器设置、UI 配置等
- **[init.el](mdc:init.el)** - 主初始化文件，通过 require 语句加载各个模块化配置
- **[custom.el](mdc:custom.el)** - Emacs 自定义配置，由 Custom 系统自动生成

## 目录结构

- **lisp/** - 自定义配置模块目录，包含所有模块化的配置文件
- **elpa/** - 第三方包安装目录
- **snippets/** - 代码片段目录
- **Fonts/** - 字体文件目录  
- **local/** - 本地缓存和数据目录
- **debug-adapters/** - 调试适配器目录
- **eshell/** - Eshell 相关配置和历史

## 配置模块加载顺序

1. **基础配置**: use-package, default, helper, theme, font, undo, autosave
2. **第三方包**: which-key, general, hydra, consult, vertico, crux
3. **编辑器功能**: editor, dired, format, jump, checker, pair, fold
4. **IDE 功能**: lsp-bridge, citre, treesit
5. **开发工具**: vc, debugger, envrc, project
6. **AI 集成**: gpt
7. **语言支持**: web, rust, python
8. **工具**: docker, quickrun, feed, lookup, terminal, reader
9. **按键绑定**: keybindings

## 环境变量

项目通过 `exec-path-from-shell` 加载环境变量，包括：
- SSH_AUTH_SOCK, SSH_AGENT_PID
- GPG_AGENT_INFO
- LANG, LC_CTYPE
- NIX_SSL_CERT_FILE, NIX_PATH
- GROQ_API_KEY, DEEPSEEK_API_KEY
